/**
 * Keyboard Key Codes.
 *
 * Used to identify keyboard event key codes and handle proper key press.
 *
 * @enum
 */
export enum Key {
  ARROW_LEFT = 'ArrowLeft',
  ARROW_RIGHT = 'ArrowRight',
  DELETE = 'Delete',
  BACKSPACE = 'Backspace',
  TAB = 'Tab',
  SHIFT = 'Shift',
  CONTROL = 'Control',
  CAPS_LOCK = 'CapsLock',
  NUM_LOCK = 'NumLock',
}
